import tkinter as tk

# Configure the window
title = "Calculator"
size = "300x400"

# Create a window
window =  tk.Tk()
window.title(title)
window.geometry(size)

# Create a frame
frame = tk.Frame(window)
frame.grid()

